[project]
name = "sentryai-agents"
version = "1.0.0"
description = "SentryAI Agent Service — LangGraph + Claude + Pinecone RAG"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.mypy]
python_version = "3.11"
strict = true
